@model IEnumerable<GraduationProject.Data.Models.Order>

@if (Model.Count() > 0)
{
    @foreach (var x in Model)
    {

        <h1>@x.OrderID</h1>
        <h1>@x.CreatedAt</h1>
    }
}
else
{ <h1>لا توجد طلبات غير محططة حالياَ</h1>}
